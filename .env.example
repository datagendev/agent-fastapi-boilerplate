# ==========================================
# Required Configuration
# ==========================================

# Anthropic API Key (Required)
# Get your key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# ==========================================
# Agent Selection
# ==========================================

# Option 1: Agent name (loads .claude/agents/{AGENT_NAME}.md)
# The boilerplate will look for .claude/agents/default.md by default
AGENT_NAME=default

# Option 2: Explicit path (overrides AGENT_NAME)
# Use this for absolute paths or custom locations
# AGENT_FILE_PATH=/app/.claude/agents/my-agent.md

# ==========================================
# Optional: MCP Integration
# ==========================================

# Datagen MCP Server API Key
# Get your key from: https://datagen.dev/account?tab=api
# DATAGEN_API_KEY=your-datagen-api-key-here

# ==========================================
# Optional: Security
# ==========================================

# Webhook Secret for API authentication
# Set this to require X-API-Key header on /run endpoint
# Generate with: openssl rand -hex 32
# WEBHOOK_SECRET=your-random-secret-key-here

# ==========================================
# Optional: Model Configuration
# ==========================================

# Model override (overrides agent.md frontmatter)
# Default: claude-sonnet-4-5
# Options: claude-sonnet-4-5, claude-opus-4-5, claude-haiku-4
# MODEL_NAME=claude-sonnet-4-5

# ==========================================
# Optional: Application Settings
# ==========================================

# Logging level
# Options: DEBUG, INFO, WARNING, ERROR
# LOG_LEVEL=INFO

# Server port (Railway sets this automatically)
# PORT=8000

# Agent SDK permission mode
# Default: bypassPermissions (safe in Docker with non-root user)
# PERMISSION_MODE=bypassPermissions

# ==========================================
# Optional: CORS Configuration
# ==========================================

# Enable CORS for frontend integrations
# CORS_ENABLED=false

# Allowed CORS origins (comma-separated)
# Use "*" for all origins (development only)
# For production, specify exact origins: https://app.example.com,https://example.com
# CORS_ORIGINS=*
